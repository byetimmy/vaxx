{
  "name": "vaxx",
  "version": "1.7.4",
  "description": "",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/cluster.js",
    "start:app": "node src/app.js",
    "build": "npm test && node build/dist.js",
    "lint": "eslint src",
    "test": "npm run lint",
    "start:docker": "docker-compose build && docker-compose --env-file .env.dev up"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.5",
    "cluster": "^0.7.7",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "os": "^0.1.1",
    "request": "^2.88.2",
    "request-promise": "^4.2.2"
  },
  "author": "byetimmy",
  "license": "",
  "devDependencies": {
    "adm-zip": "^0.5.5",
    "eslint": "^8.1.0",
    "fs-extra": "^9.1.0"
  }
}
